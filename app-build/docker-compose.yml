# Only for local testing
version: "3"
services:
  app:
    build: 
      context: ./../
      dockerfile: ./app-build/Dockerfile
    tty: true
    #user: solr
    ports:
      - "8080:8080"
    environment:
      - RUN_IN_LOCAL=true
    volumes: 
      - "./../data:/data"